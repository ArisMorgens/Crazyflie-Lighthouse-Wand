[project]
name = "lighthouse-wand"
version = "0.1.0"
description = "Tools for the Lighthouse wand."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cflib>=0.1.32",
    "colorama>=0.4.6",
    "pyyaml>=6.0",
    "tqdm>=4.0.0",
]
